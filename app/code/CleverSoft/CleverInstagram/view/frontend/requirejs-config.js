var config = {
    paths: {
        "instafeed.min": "CleverSoft_CleverInstagram/js/instafeed.min"
    },
    shim: {
        "instafeed.min": {
            deps: ["jquery"]
        }
    }
};